---

- hosts: all
  become: true
  tasks:

  
  - name: update repository index
    yum:
      update_cache: yes  

#   when: ansible_distribusion == "CentOS" and ansible_distribusion_version == "8.2"  
#   when: ansible_distribusion in ["Debian", "Ubuntu"]

  - name: install apache2 package
    yum:
      name: apache2
      state: latest

  - name: add php support for apache
    yum: 
      name: libapache2-mod-php
      state: latest

